spring:
    application:
        name: user
    datasource:
        url: jdbc:postgresql://db:5432/user
        username: postgres
        password: user123
    jpa:
        hibernate:
            ddl-auto: update